import React from 'react'
import { Text, View } from 'react-native'

const recentrans = () => {
  return (
    <View>
        <Text className='text-text-primary font-p'>No Recent Transcation</Text>
    </View>
  )
}

export default recentrans

